

const TrailerPlayer = () => {
    return (
        <div>
            {/* <ReactPlayer
                className="relative z-50"
                url={filemoon}
                controls={true}
                onPlay={handlePlay}
                onError={handleError}
                onProgress={handleProgress}
                onReady={handleReady}
                width="800px"
                height="450px"
                config={{
                    youtube: {
                        playerVars: { showinfo: 1 }
                    },
                    file: {
                        attributes: {}
                    }
                }}
            /> */}
        </div>
    )
}

export default TrailerPlayer
